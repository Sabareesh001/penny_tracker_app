import { View } from "react-native";
import SpendingHistory from "../dashboard/spendingHistory";

export default function Payments(){
    return (
        <View style={{padding:16}}>
            <SpendingHistory/>
        </View>
    )

}